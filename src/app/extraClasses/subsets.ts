import { Counter } from './counters';
import { Dimention } from 'app/extraClasses/Dimention';

export class Subset {
  release: string;
  unv: string;
  subsetID: string;
  measurements: Counter[];
  dimentions: Dimention[];
  // Type: string;
  // DataType: string;
  // ListName: string;
}
